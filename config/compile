Lancer la commande :
pyinstaller main.py --onefile  --add-binary "driver/chromedriver;driver/" --add-data "example.ini;." --name sport_analyse
pyinstaller -F -w --hidden-import=tkinter --hidden-import=tkinter.filedialog main.py

A la fin du fichier .spec, pour ajouter le fichier "example.ini":

import shutil
shutil.copyfile('example.ini', '{0}/example.init'.format(DISTPATH))

Puis lancer la commande :
pyinstaller --clean sport_analyse.spec